```markdown
# 农产品行情采集分析系统

基于Python的农产品价格数据采集与分析系统，支持多品类数据爬取、清洗、存储、统计分析和可视化展示。项目采用模块化设计，核心流程独立成pipeline，GUI与命令行均可复用。

## 功能特点

- **智能爬虫**：基于Playwright，支持随机延时、Cookie模拟、登录弹窗检测
- **数据清洗**：自动去重、空值处理、按品类3σ异常值过滤、价格区间划分
- **统计分析**：多维度统计（品类核心指标、价格区间分布、产地覆盖、省份均价Top5）
- **数据可视化**：6种图表自动生成（柱状图、饼图、箱线图、词云等）
- **数据存储**：CSV文件 + MongoDB数据库双存储
- **GUI界面**：友好的Tkinter操作界面
- **测试保障**：单元测试 + 集成测试，使用独立测试库

## 项目结构

```
agri_price_spider/
├── main.py                 # 程序入口（GUI启动）
├── pipeline.py             # 核心流程编排
├── config/
│   ├── __init__.py
│   ├── settings.py         # 全局配置
│   └── selectors.py        # 页面选择器配置
├── core/
│   ├── __init__.py
│   ├── spider.py           # 爬虫模块
│   ├── analysis.py         # 数据分析
│   ├── database.py         # 数据库操作
│   ├── visualizer.py       # 可视化
│   ├── excel_report.py     # Excel报告生成
│   └── gui.py              # GUI界面
├── utils/
│   ├── __init__.py
│   └── data_tools.py       # 工具函数
├── tests/
│   ├── conftest.py         # 测试夹具
│   ├── test_data_tools.py  # 工具函数测试
│   ├── test_analysis_business.py  # 分析逻辑测试
│   ├── test_pipeline.py    # 流程容错测试
│   └── test_integration.py # 数据库集成测试
├── assets/
│   └── output/             # 输出目录（CSV、Excel、图表）
├── requirements.txt        # 依赖列表
├── .gitignore
└── README.md
```

## 环境要求

- Python 3.8+
- MongoDB 4.0+
- Chrome浏览器（Playwright需要）

## 安装步骤

### 1. 克隆项目

```bash
git clone https://github.com/ABCCD-jpg/hnb-agriculture-analysis.git
cd hnb-agriculture-analysis
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

### 3. 安装Playwright浏览器

```bash
playwright install chromium
```

### 4. 配置环境变量

创建 `.env` 文件：

```env
LOGIN_COOKIE=你的Cookie
MONGO_URI=mongodb://localhost:27017
```

### 5. 启动MongoDB

确保MongoDB服务已启动。

## 启动方式

### GUI启动

```bash
python main.py
```

### 命令行启动

```bash
python -c "from pipeline import run_pipeline; result = run_pipeline(['苹果', '香蕉'], 2); print(result)"
```

## 运行测试

```bash
# 运行所有测试
pytest -v

# 运行特定测试文件
pytest tests/test_data_tools.py -v

# 运行特定测试函数
pytest tests/test_analysis_business.py::test_outlier_filtering_works -v
```

## 技术栈

- **爬虫**：Playwright
- **数据处理**：Pandas
- **数据库**：MongoDB
- **可视化**：Matplotlib + Seaborn + WordCloud
- **GUI**：Tkinter
- **测试**：Pytest

## 核心设计

### Pipeline模式
核心流程独立成 `pipeline.py`，GUI和命令行均可复用。

### 失败隔离
单个品类爬取失败不会中断整体流程，失败品类会被记录并跳过。

### 测试保障
- 单元测试：验证工具函数正确性
- 集成测试：验证数据流完整性
- 容错测试：验证异常处理逻辑